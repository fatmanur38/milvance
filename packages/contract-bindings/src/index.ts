/**
 * @milvance/contract-bindings — generated MilvanceCore TypeScript bindings.
 *
 * ⚠️ PKG-00 placeholder. Bindings are **generated in PKG-05** from the deployed
 * testnet contract via the Stellar CLI, and re-exported from here.
 *
 * Generated output belongs in `src/generated/` (lint-ignored). Do not hand-edit
 * generated files: regenerate them from the deployed contract instead, so the
 * bindings can never drift from on-chain truth.
 */

/** Marker export so the package has a stable, importable surface before PKG-05. */
export const CONTRACT_BINDINGS_GENERATED = false as const;
